import { getFreeAgentMarketTerms } from '@/app/market'
import { getPlayerAge, getPlayerPotentialBand } from '@/domain/player'
import { canTeamAffordAdditionalSalary, getFreeAgents, getTeamFinancialSnapshot, type GameWorld } from '@/domain/world'
import { getUserTeam } from '@/engine/calendar'
import type { PlayerId } from '@/domain/ids'
import { formatMoney } from '@/ui/formatters'
export function MarketScreen({world,onSign}:{readonly world:GameWorld;readonly onSign:(playerId:PlayerId)=>void}){const team=getUserTeam(world);if(!team)return null;const finances=getTeamFinancialSnapshot(world,team.id);const agents=getFreeAgents(world);return <section className="screen"><div className="page-heading"><div><p className="eyebrow">MARKET</p><h1>FREE AGENTS</h1></div></div><div className="content-panel"><p>SALARY BUDGET {formatMoney(finances.playerSalaryBudget)} · PAYROLL {formatMoney(finances.currentPlayerPayroll)} · REMAINING {formatMoney(finances.remainingPlayerSalaryBudget)}</p></div>{agents.length===0?<p className="content-panel">NO FREE AGENTS AVAILABLE</p>:<div className="content-panel table-wrap"><table><thead><tr><th>PLAYER</th><th>POS</th><th>AGE</th><th>POT</th><th>FIN</th><th>SHT</th><th>PLY</th><th>PD</th><th>ID</th><th>REB</th><th>ATH</th><th>ASKING</th><th>TERM</th><th>ACTION</th></tr></thead><tbody>{agents.map(player=>{const terms=getFreeAgentMarketTerms(world,player.id);const ok=canTeamAffordAdditionalSalary(world,team.id,terms.annualSalary);const r=player.basketball.ratings;return <tr key={player.id}><td>{player.firstName} {player.lastName}</td><td>{player.basketball.primaryPosition}</td><td>{getPlayerAge(world,player.id)}</td><td>{getPlayerPotentialBand(player.potential).toUpperCase()}</td><td>{r.finishing}</td><td>{r.shooting}</td><td>{r.playmaking}</td><td>{r.perimeterDefense}</td><td>{r.interiorDefense}</td><td>{r.rebounding}</td><td>{r.athleticism}</td><td>{formatMoney(terms.annualSalary)}</td><td>{terms.contractYears} YRS</td><td><button disabled={!ok} onClick={()=>onSign(player.id)} type="button">{ok?'SIGN':'INSUFFICIENT SALARY BUDGET'}</button></td></tr>})}</tbody></table></div>}</section>}
