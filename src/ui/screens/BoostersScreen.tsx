import type { GameWorld } from '@/domain/world'
import { getUserTeam } from '@/engine/calendar'
export function BoostersScreen({world,onSupport}:{world:GameWorld;onSupport:(id:string)=>void}){const team=getUserTeam(world);const boosters=Object.values(world.boostersById).filter(b=>b.programTeamId===team?.id),contributions=Object.values(world.boosterContributionsById);return <section className="screen"><div className="page-heading"><div><p className="eyebrow">Donors & Boosters</p><h1>Apoyo al programa</h1></div></div><div className="table-wrap"><table><thead><tr><th>BOOSTER</th><th>INFLUENCE</th><th>RELATIONSHIP</th><th>AGENDA</th><th>RESOURCES</th><th>COLLECTIVE / SUPPORT</th></tr></thead><tbody>{boosters.map(b=>{const c=Object.values(world.collectivesById).find(x=>x.programTeamId===b.programTeamId);return <tr key={b.id}><td>{b.name}</td><td>{b.influence}</td><td>{b.relationship}</td><td>{b.agenda}</td><td>{b.resourcesRemaining}/{b.resourceCapacity}</td><td>{c?.name} · {contributions.filter(x=>x.boosterId===b.id).length} <button className="text-button" onClick={()=>onSupport(b.id)} type="button">Request support</button></td></tr>})}</tbody></table></div></section>}
