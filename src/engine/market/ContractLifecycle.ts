import type { GameDate } from '@/domain/date'
import { getPlayerContractStatus } from '@/domain/contract'
import { playerTransactionIdFromString } from '@/domain/ids'
import { createGameWorld, type GameWorld } from '@/domain/world'
export function reconcileExpiredPlayerContracts(world:GameWorld,onDate:GameDate):GameWorld{const removals=new Map<string,string>();const transactions=[];for(const team of Object.values(world.teams))for(const playerId of team.rosterPlayerIds){const contracts=Object.values(world.contractsById).filter(c=>c.playerId===playerId&&c.teamId===team.id);if(contracts.some(c=>['active','scheduled'].includes(getPlayerContractStatus(c,onDate))))continue;const expired=contracts.filter(c=>getPlayerContractStatus(c,onDate)==='expired').sort((a,b)=>b.term.expiresOn.localeCompare(a.term.expiresOn))[0];if(expired){removals.set(playerId,team.id);const id=playerTransactionIdFromString(`transaction:contractExpired:${expired.id}`);if(world.playerTransactionsById[id]===undefined)transactions.push({id,playerId,kind:'contractExpired' as const,occurredOn:expired.term.expiresOn,fromTeamId:team.id,contractId:expired.id})}}if(!removals.size&&!transactions.length)return world;return createGameWorld({currentDate:world.currentDate,currentSeasonId:world.currentSeasonId,userCoachId:world.userCoachId,countries:Object.values(world.countries),coaches:Object.values(world.coaches),players:Object.values(world.players),teams:Object.values(world.teams).map(t=>({...t,rosterPlayerIds:t.rosterPlayerIds.filter(id=>!removals.has(id))})),competitions:Object.values(world.competitions),seasons:Object.values(world.seasons),games:Object.values(world.games),matchStatLogs:Object.values(world.matchStatLogsByGameId),seasonHistory:Object.values(world.seasonHistoryBySeasonId),injuries:Object.values(world.injuriesById),contracts:Object.values(world.contractsById),teamFinances:Object.values(world.teamFinancesByTeamId),playerTransactions:[...Object.values(world.playerTransactionsById),...transactions],playerKnowledge:Object.values(world.playerKnowledgeById),staffPeople:Object.values(world.staffPeopleById),teamStaffAssignments:Object.values(world.teamStaffAssignmentsById),coachProfessionalProfilesByCoachId:world.coachProfessionalProfilesByCoachId,coachRpgProfilesByCoachId: world.coachRpgProfilesByCoachId, coachReputationProfilesByCoachId: world.coachReputationProfilesByCoachId, coachEmploymentByCoachId: world.coachEmploymentByCoachId, coachCareerHistoryByCoachId: world.coachCareerHistoryByCoachId, coachJobOpeningsById: world.coachJobOpeningsById, coachJobCandidaciesById: world.coachJobCandidaciesById, coachInterviewsByCandidacyId: world.coachInterviewsByCandidacyId, coachJobOffersById: world.coachJobOffersById, relationshipsByKey: world.relationshipsByKey})}
