import{describe,it,expect}from'vitest';import{applyCoachReputationEvent,createDefaultCoachReputationProfile,getCoachReputationBand,COACH_REPUTATION_DIMENSIONS}from'./CoachReputation';const e=(id='e',source='matchResult' as const)=>({id,gameDate:'2030-01-01',source,deltas:{competitive:10},context:{kind:source,key:'x'}})
describe('Coach reputation core',()=>{it('has four independent default dimensions',()=>{expect(COACH_REPUTATION_DIMENSIONS).toEqual(['competitive','development','professional','publicStanding']);expect(createDefaultCoachReputationProfile()).toEqual({values:{competitive:200,development:200,professional:200,publicStanding:200},events:[]})});it('applies, clamps and deduplicates immutable events',()=>{const p=createDefaultCoachReputationProfile(),x=applyCoachReputationEvent(p,{...e(),deltas:{competitive:10000,professional:-10000}});expect(x).toMatchObject({ok:true,applied:true});if(!x.ok||!x.applied)return;expect(x.profile.values).toMatchObject({competitive:1000,professional:0});expect(applyCoachReputationEvent(x.profile,e())).toMatchObject({applied:false,reason:'duplicateEvent'});expect(p.events).toEqual([])});it('rejects invalid inputs and resolves bands',()=>{expect(applyCoachReputationEvent(createDefaultCoachReputationProfile(),{...e(' ')})).toMatchObject({ok:false,reason:'invalidEventId'});expect(applyCoachReputationEvent(createDefaultCoachReputationProfile(),{...e(),context:{kind:'publicEvent',key:'x'}})).toMatchObject({ok:false,reason:'invalidContext'});expect(getCoachReputationBand(0)).toBe('unknown');expect(getCoachReputationBand(100)).toBe('emerging');expect(getCoachReputationBand(900)).toBe('legendary')})})
