import { CANONICAL_RATING_KEYS, type BasketballRatingKey, type CanonicalRatingKey } from '@/domain/player'
export type DevelopmentStimulusRatings = Readonly<Record<CanonicalRatingKey,number> & Partial<Record<BasketballRatingKey,number>>>
export interface PlayerDevelopmentStimulus{readonly playerId:string;readonly byRating:DevelopmentStimulusRatings}
export const EMPTY_DEVELOPMENT_STIMULUS:Readonly<Record<CanonicalRatingKey,number>>=Object.freeze(Object.fromEntries(CANONICAL_RATING_KEYS.map((key)=>[key,0])) as Record<CanonicalRatingKey,number>)
export function addDevelopmentStimulus(current:PlayerDevelopmentStimulus,addition:Readonly<Partial<Record<CanonicalRatingKey,number>>>):PlayerDevelopmentStimulus{const byRating={...current.byRating};for(const key of CANONICAL_RATING_KEYS){const value=addition[key]??0;if(!Number.isFinite(value)||value<0)throw new RangeError('Development stimulus must be finite and non-negative');byRating[key]+=value}return{playerId:current.playerId,byRating}}
/** V1-only distribution. The returned storage has only canonical keys. */
export function migrateLegacyDevelopmentStimulus(legacy: Readonly<Record<BasketballRatingKey, number>>): DevelopmentStimulusRatings { const groups: Readonly<Record<BasketballRatingKey, readonly CanonicalRatingKey[]>> = { finishing:['rimFinishing','contactFinishing','dunking','floater','postScoring'], shooting:['midRangeShooting','threePointShooting','freeThrowShooting'], playmaking:['ballHandling','ballSecurity','firstStep','passing','courtVision','decisionMaking','offBallAwareness'], perimeterDefense:['perimeterDefense','screenNavigation','defensiveAwareness','steal','shotContest','lateralAgility'], interiorDefense:['interiorDefense','rimProtection','shotContest','strength','defensiveAwareness'], rebounding:['offensiveRebounding','defensiveRebounding','boxOut','strength','vertical'], athleticism:['acceleration','speed','lateralAgility','strength','vertical','stamina','changeOfDirection'] }; const result: Record<CanonicalRatingKey, number> = Object.fromEntries(CANONICAL_RATING_KEYS.map((key)=>[key,0])) as Record<CanonicalRatingKey,number>; for (const key of Object.keys(groups) as BasketballRatingKey[]) for (const target of groups[key]) result[target] += legacy[key] / groups[key].length; return result }
