# Milestone 016B.1 - Enforce valid MatchEngine lineups

Expected code ancestor: `f676d25 feat: attribute match actions to players`

## Goal

Make valid lineups a real MatchEngine invariant before the Viewer consumes PlayerIds.

## Required changes

- `SimulateMatchOptions.lineups` must be required for detailed simulation.
- Each side must contain exactly 5 PlayerIds.
- IDs must be unique within each side.
- Home and away may not share any PlayerId.
- MatchEngine must never generate/select a lineup internally.
- If `actorRandom` is optional only for historical compatibility, make it required for any path that generates player-attributed sporting events. Never fall back to the sporting RNG for actor selection.
- Update every direct MatchEngine test that still uses empty or invalid lineups. Use reusable 5-player HOME_LINEUP and AWAY_LINEUP fixtures.
- Add focused tests for:
  - home length 4 rejected
  - home length 6 rejected
  - away length 4 rejected
  - duplicate home rejected
  - duplicate away rejected
  - same PlayerId in home/away rejected
  - sporting-event PlayerId belongs to the correct lineup
  - same sporting RNG + different actor RNG keeps finalScore, clocks, periods, event types, team attribution and points identical when PlayerId is projected away
- Do not weaken Application. Its production path already supplies real Starting Fives and actor RNG.
- Do not change MatchViewer.
- Do not change basketball outcomes intentionally.
- Do not add player stats.

## Validation

Run the repository Definition of Done in AGENTS.md.

Also search direct MatchEngine tests for `home: []` / `away: []` or equivalent. Expected zero.

## Commit

`test: enforce valid match lineups`

Stop after this commit.
